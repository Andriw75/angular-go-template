SERVER_PORT=:8080
USE_MOCK=true

# Database settings (only used when USE_MOCK=false)
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mydb

# JWT settings
JWT_SECRET=mi-secreto-super-seguro-2024
JWT_EXPIRATION_MINUTES=15
JWT_RENEW_MINUTES=5

# Cookie settings
COOKIE_NAME=access_token_go
COOKIE_SECURE=false

# CORS
CORS_ORIGIN=http://localhost:4200
